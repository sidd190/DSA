#include <iostream>
#include <bits/stdc++.h>
using namespace std;

vector<int> maxSlidingWindow(vector<int>& nums, int k) {
    deque<int> x(nums.begin(), nums.begin()+k);
    vector<int> maxSlider{0};
    for(int i = 0; i < nums.size(); i++){
        maxSlider.push_back(max_iterator);

    }
}

int main() {
    
    return 0;
}